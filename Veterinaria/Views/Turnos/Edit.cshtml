

@model LogicaDeNegocio.Models.Turno

@{
    ViewData["Title"] = "Editar Turno";
}
<h1 class="fw-bold text-dark mb-4">Editar</h1>

<div class="row justify-content-md-start justify-content-center">
    <div class="col-md-8 col-lg-6">
        <div class="card border-0 shadow-sm colorful-form-card">
            <div class="card-header colorful-form-header">
                <h5 class="card-title mb-0 text-white">
                    <i class="fas fa-edit me-2"></i>Editar Turno
                </h5>
            </div>
            <div class="card-body p-4">
                <form asp-action="Edit" method="post">
                    <div asp-validation-summary="ModelOnly" class="alert alert-danger"></div>
                    <input type="hidden" asp-for="Id" />

                    <div class="form-group mb-4">
                        <label asp-for="FechaHora" class="form-label fw-semibold"></label>
                        <input type="datetime-local" asp-for="FechaHora" class="form-control" />
                        <span asp-validation-for="FechaHora" class="text-danger small"></span>
                    </div>

                    <div class="form-group mb-4">
                        <label asp-for="EstadoTurno" class="form-label fw-semibold"></label>
                        <select asp-for="EstadoTurno" class="form-control" asp-items="ViewBag.Estados"></select>
                        <span asp-validation-for="EstadoTurno" class="text-danger small"></span>
                    </div>

                    <div class="form-group mb-4">
                        <label asp-for="MascotaId" class="form-label fw-semibold"></label>
                        <select asp-for="MascotaId" class="form-control" asp-items="ViewBag.MascotaId">
                            <option value="" class="text-center">--- Seleccione una mascota ---</option>
                        </select>
                        <span asp-validation-for="MascotaId" class="text-danger small"></span>
                    </div>

                    <div class="form-group mb-3">
                        <button type="submit" class="btn btn-primary colorful-btn-hover w-100 py-2">
                            <i class="fas fa-save me-2"></i>Guardar Cambios
                        </button>
                    </div>
                </form>
            </div>
            <div class="card-footer colorful-form-footer">
                <a asp-action="Index" class="btn btn-secondary colorful-btn-hover">
                    <i class="fas fa-arrow-left me-2"></i>Volver a la lista
                </a>
            </div>
        </div>
    </div>
</div>
@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
